import 'package:flutter/material.dart';

import 'profile_identity/profile_identity_tab_view.dart';

class ProfileIdentityTab extends StatelessWidget {
  const ProfileIdentityTab({super.key});

  @override
  Widget build(BuildContext context) => const ProfileIdentityTabView();
}
